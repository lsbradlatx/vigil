# Required: PostgreSQL (schema uses postgresql; do not use file:./dev.db)
DATABASE_URL="postgresql://user:password@localhost:5432/vigil"

# Required: NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Required: Resend API key for email verification (https://resend.com)
RESEND_API_KEY=""

# Optional: Custom "from" address for emails (requires verified domain in Resend)
# EMAIL_FROM="Vigil <noreply@yourdomain.com>"

# Optional: Google Calendar (Connect Google Calendar on the Calendar page)
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
# GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

# Asana uses an in-app Personal Access Token (no env vars).
