# Required: PostgreSQL (schema uses postgresql; do not use file:./dev.db)
DATABASE_URL="postgresql://user:password@localhost:5432/vigil"

# Required: NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=""
# App URL (used for verification links in emails and redirects). Use your Railway URL in production.
NEXTAUTH_URL="http://localhost:3000"
# Required on Railway/production so Auth.js trusts the request host (set to true).
AUTH_TRUST_HOST="true"

# Required for verification emails (https://resend.com). Create an API key in the Resend dashboard.
# To send to any email address you must add and verify a domain in Resend; otherwise delivery is limited.
RESEND_API_KEY=""

# Optional: Custom "from" address (requires a verified domain in Resend). If unset, uses onboarding@resend.dev (limited delivery).
# EMAIL_FROM="Vigil <noreply@yourdomain.com>"

# Optional: Google Calendar (Connect Google Calendar on the Calendar page)
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
# GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

# Asana uses an in-app Personal Access Token (no env vars).

# Optional: One-time wipe of all user data (POST /api/admin/wipe-users with ?secret=THIS_VALUE).
# Set temporarily to reset accounts, then remove or leave unset.
# WIPE_SECRET=""
